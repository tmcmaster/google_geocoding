name: example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cupertino_icons:
  flutter:
    sdk: flutter
  flutter_dotenv: 5.2.1
  google_geocoding:
    git:
      url: git@github.com:tmcmaster/google_geocoding.git
  google_place:

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  google_geocoding:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env
